spring:
  datasource:
    url: jdbc:mysql://YOUR_PROD_DB_IP:3306/pitaya?useUnicode=true&useSSL=false&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: pitaya_prod
    password: ${MYSQL_PASSWORD}

  data:
    redis:
      host: YOUR_PROD_REDIS_IP
      port: 6379
      password: ${REDIS_PASSWORD}

# 这里是以后正式部署在服务器，使用系统的环境变量